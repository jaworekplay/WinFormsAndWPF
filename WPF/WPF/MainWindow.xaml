<Window x:Class="WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ViewModel;assembly=ViewModel"
        xmlns:local="clr-namespace:WPF"
        xmlns:uc="clr-namespace:WPF_UserControls;assembly=WPF_UserControls"
        xmlns:printing="clr-namespace:WPF_UserControls.Printing;assembly=WPF_UserControls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <DataTemplate x:Key="dtUser">
            <uc:User/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:LoanConfirmationViewModel}">
            <printing:LoanConfirmation/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ContentControl Grid.ColumnSpan="5" Grid.RowSpan="3" Content="{Binding CurrentViewModel}"/>
        <!--<ListView x:Name="Users" Grid.ColumnSpan="5" Grid.RowSpan="3" ItemsSource="{Binding Users}" SelectedValue="{Binding SelectedUser}" ItemTemplate="{StaticResource dtUser}"/>-->
        <Button Content="Save" Grid.Row="3"/>
        <Button Content="Edit" Grid.Row="3" Grid.Column="1"/>
        <Button Content="Create" Grid.Row="3" Grid.Column="2"/>
        <Button Content="Update" Grid.Row="3" Grid.Column="3"/>
        <Button Content="Delete" Grid.Row="3" Grid.Column="4" Command="{Binding DeleteCommand}" CommandParameter="{Binding ElementName=Users, Path=SelectedItems}"/>
    </Grid>
</Window>
